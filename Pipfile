[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[scripts]
scrape = "pipenv run python scrape.py"
scrape-threaded = "pipenv run python scrape_threaded.py"

[packages]
requests = "*"

[dev-packages]

[requires]
python_version = "3.10"
